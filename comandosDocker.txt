Ver en pagina:
http://localhost:8080

#Apagar todo pero conservar los datos de las bases de datos: 
    docker compose down

#Apagar todo y borrar los datos:
    docker compose down -v

#Encender los servicios
    -Si ya estaban construidos antes:
     docker compose up

    -Si modificaste algún archivo de código:
     docker compose up --build

Ver registros
docker exec -it db-pacientes mysql -u root -p
SHOW DATABASES;
USE paciente_db;
SHOW TABLES;
SELECT * FROM pacientes;

{email=admin@mediturnos.com; password=admin1234}

usuario
sneyder@gmail.com
angel1234

medico 
Juan.perez@mediturnos.com
juan1234

iniciar pruebas 
npm run test:coverage

inicar ngrok 
Get-ChildItem -Path . -Recurse -Include "*.html" | ForEach-Object { (Get-Content $_.FullName) -replace 'https://movable-raging-ice.ngrok-free.dev', 'https://NUEVA-URL-AQUI.ngrok-free.app' | Set-Content $_.FullName

actualizar git hub 
git add .
git commit -m "fix: actualizar URL ngrok"
git push origin main

ngrok 
ngrok config add-authtoken 3DHZAEPbdCdaAZ2O2iC7zQ0RbHu_7P2dBdeLapb3DiVNwxc1d
ngrok http 80